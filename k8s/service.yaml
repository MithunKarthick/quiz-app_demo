apiVersion: v1
kind: Service
metadata:
  name: quiz-app
  labels:
    app: quiz-app
spec:
  type: NodePort
  selector:
    app: quiz-app
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
